require 'spec_helper'

describe Crossover do
  it 'has a version number' do
    expect(Crossover::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
